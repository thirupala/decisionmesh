{
  "_readme": [
    "This is a TEMPLATE. Copy it to bao_secrets.prod.json or bao_secrets.dev.json",
    "and fill in real values. NEVER commit a filled-in copy to git.",
    "",
    "Key names inside 'data' must be valid Quarkus config property names so that",
    "quarkus.vault.secret-config-kv-path injects them automatically at startup.",
    "",
    "After filling in values, import with:",
    "  Linux : ./import-bao-secrets.sh --file bao_secrets.prod.json --token <token>",
    "  Windows: .\\import-bao-secrets.ps1 -InputFile bao_secrets.prod.json -Token <token>"
  ],
  "exported_at": "REPLACE_WITH_DATE",
  "bao_address": "http://localhost:8200",
  "root_path": "secret/",
  "total_paths": 8,
  "secrets": {

    "secret/decisionmesh/db": {
      "path": "secret/decisionmesh/db",
      "kv_version": "v2",
      "data": {
        "quarkus.datasource.username": "REPLACE_ME",
        "quarkus.datasource.password": "REPLACE_ME"
      }
    },

    "secret/decisionmesh/redis": {
      "path": "secret/decisionmesh/redis",
      "kv_version": "v2",
      "data": {
        "quarkus.redis.hosts": "redis://:REPLACE_ME@redis:6379"
      }
    },

    "secret/decisionmesh/llm": {
      "path": "secret/decisionmesh/llm",
      "kv_version": "v2",
      "data": {
        "llm.anthropic.api-key": "REPLACE_ME",
        "llm.openai.api-key":    "REPLACE_ME",
        "llm.gemini.api-key":    "REPLACE_ME",
        "llm.deepseek.api-key":  "REPLACE_ME"
      }
    },

    "secret/decisionmesh/email": {
      "path": "secret/decisionmesh/email",
      "kv_version": "v2",
      "data": {
        "quarkus.mailer.username": "REPLACE_ME@gmail.com",
        "quarkus.mailer.password": "REPLACE_ME_APP_PASSWORD"
      }
    },

    "secret/decisionmesh/zitadel": {
      "path": "secret/decisionmesh/zitadel",
      "kv_version": "v2",
      "data": {
        "zitadel.organization-id":      "REPLACE_ME",
        "zitadel.project-id":           "REPLACE_ME",
        "zitadel.service-account-token": "REPLACE_ME"
      }
    },

    "secret/decisionmesh/auth": {
      "path": "secret/decisionmesh/auth",
      "kv_version": "v2",
      "data": {
        "quarkus.oidc.credentials.secret": "REPLACE_ME_IF_NEEDED_OR_REMOVE"
      }
    },

    "secret/decisionmesh/billing": {
      "path": "secret/decisionmesh/billing",
      "kv_version": "v2",
      "data": {
        "stripe.secret.key":         "REPLACE_ME",
        "stripe.webhook.secret":     "REPLACE_ME",
        "razorpay.key.id":           "REPLACE_ME",
        "razorpay.key.secret":       "REPLACE_ME",
        "razorpay.webhook.secret":   "REPLACE_ME"
      }
    },

    "secret/decisionmesh/kafka": {
      "path": "secret/decisionmesh/kafka",
      "kv_version": "v2",
      "data": {
        "_note": "No secrets needed for internal Kafka. Placeholder to satisfy vault path config."
      }
    }

  }
}
