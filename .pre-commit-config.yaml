repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
        args:
          [
            "--config=.gitleaks.toml",
            "--verbose",
            "--redact",
            "--no-git",
            "--source=.",
          ]